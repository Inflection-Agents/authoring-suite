---
description: Realign a finished set to a renamed concept or to shipped code
argument-hint: [what changed, or a path to the new source]
---

Use the `arch-docs` skill and load `references/12-late-change.md`.

Record the new facts in the facts file first, in a dated section that names which earlier line each one corrects.
Map the owner's diagram numbers by slug against the current manifest. Delete stale pages and images for renamed
slugs. Report every place the new source contradicted the earlier facts; that list is the point of the exercise.
