---
description: Write the facts file that every later phase and every build agent reads
---

Use the `arch-docs` skill and load `references/04-facts-file.md`.

Write `_brief/facts.md` with the decisions section on top, the provenance marks, the open items and the vocabulary.
Show it to the owner before anything is drawn. Their corrections are binding on every later diagram.
