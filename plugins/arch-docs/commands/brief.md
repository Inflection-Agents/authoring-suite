---
description: Turn the approved diagram list into the build brief every agent reads
---

Use the `arch-docs` skill and load `references/06-build-brief.md`.

Write `_brief/brief.md`: frame, files, the four-point definition of done, the library cheat sheet, the conventions
that fix connector meanings and the element registry, the voice lines, and one paragraph per diagram. Write it so
two agents drawing adjacent pages produce work that looks like one person did it.
