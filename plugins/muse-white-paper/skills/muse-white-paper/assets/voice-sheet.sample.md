# Voice Sheet Sample

Two sample entries following `references/voice-sheet-schema.md`.
Placeholder lines only; replace with the owner's own kept lines.

Rejected lines below break a rule on purpose, so they read as
defects by design. Do not run `verify-voice.py` against this file
and treat the flags as problems.

## Entry 1

- `habit`: grounds claims in a number or named artifact.
- `liked`: Failures fell 40 percent after the rollback.
- `rejected`: Failures fell significantly after the rollback.
- `scope`: cross-register; heard in the explainer and the work story.
  The single differing phrase is the number, which is the habit.

## Entry 2

- `habit`: states the claim first, no throat-clearing.
- `liked`: The retry storm caused the outage.
- `rejected`: To be clear, what happened here is that the retry
  storm caused the outage.
- `scope`: cross-register; heard in the opinion talk-through and
  the work story. The single differing phrase is the opener,
  which is the habit.
