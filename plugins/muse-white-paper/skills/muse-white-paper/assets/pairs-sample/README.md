# Pairs Sample

One stored pair following `references/edit-pairs.md`.
Placeholder lines only; the rewrite breaks the sheet habit on purpose.
Do not run `verify-voice.py` against this file and treat the flags
as problems.

## Draft (`pairs/2026-09-12-latency-draft.md`)

Failures fell 40 percent after the rollback.
The retry storm caused the outage.
The fix ships with the next deploy.

## Rewrite (`pairs/2026-09-12-latency-rewrite.md`)

Failures fell after the rollback.
The retry storm caused the outage.
The fix ships with the next deploy.

## Override note

Rewrite drops the number the sheet habit requires; the rewrite wins.
